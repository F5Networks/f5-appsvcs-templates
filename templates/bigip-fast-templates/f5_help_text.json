{
  "$schema": "http://json-schema.org/draft-07/hyper-schema",
  "definitions": {
    "tls_prereq": {
      "title": "Prerequisite",
      "description": "To configure encryption, an SSL certificate and key pair for each fully-qualified DNS name related to this application instance must be configured on the system prior to using this template. See Local Traffic >> SSL Certificate List.",
      "format": "info"
    }
  }
}